# Terraform Reference Templates

These Terraform files are documentation-oriented skeletons for a future Azure implementation. They
must not be applied to a real subscription without review, secure backend configuration,
environment-specific variables, policy checks, and approval.

The examples avoid real tenant IDs, subscription IDs, secrets, and production endpoints.

